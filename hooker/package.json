{
    "name": "hooker",
    "version": "1.0.0",
    "description": "Calls on native hooker executable to inject overlay DLL",
    "main": "index.js",
    "scripts": {
        "build": "node-gyp configure && node-gyp build",
        "clean": "node-gyp clean"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
  "devDependencies": {
    "node-addon-api": "^2.0.0",
    "node-gyp": "^6.1.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  }
}
